{
  "name": "com.outsystems.plugins.fileviewer",
  "version": "0.0.1",
  "description": "iOS Unit Tests for the plugin OSFileViewer",
  "dependencies": {
    "cordova-ios": "^4.3.0"
  },
  "scripts": {
    "test": "xcodebuild -scheme Cordova && xcodebuild test -scheme OSFileViewerLibTests -destination 'platform=iOS Simulator,name=iPhone 6' | xcpretty --report junit --report html && exit ${PIPESTATUS[0]}"
  },
  "author": "OutSystems Inc"
}
